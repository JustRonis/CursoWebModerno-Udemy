const a = 7 //atribuicao
let b = 3

b += a // equivalente b = b + a 

b -= 4 // equivalente b = b - 4

b *= 2 // b = b * 2

b /= 2 // b = b / 2

b %= 2 // b = b % 2  --- Pega o resto da divisão "módulo"  
